/target
/tools
.env
.DS_Store
/dist
# minisign secret keys never belong here
*.key
# Server blocks deploy/install.sh --proxy nginx writes for a domain
/deploy/nginx-*.conf
# install.sh rewriting .env (a copy of every secret while it runs)
.env.??????
# Claude Code agent worktrees and local launch settings (.claude/agents is tracked)
/.claude/worktrees/
/.claude/launch.json
