[toolchain]
# Pinned so CI, Docker (deploy/Dockerfile) and developers build with the
# same compiler. Bump both together.
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = ["wasm32-wasip2"]
